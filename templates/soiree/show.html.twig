{% extends 'base.html.twig' %}

{% block title %}Soiree{% endblock %}

{% block body %}
    <h1>Soiree</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soiree.id }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ soiree.day ? soiree.day|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ soiree.time ? soiree.time|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ soiree.end ? soiree.end|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ soiree.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('soiree_index') }}">back to list</a>

    <a href="{{ path('soiree_edit', {'id': soiree.id}) }}">edit</a>

    {{ include('soiree/_delete_form.html.twig') }}
{% endblock %}
